import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    background: { default: '#fcf1e6' },
    primary: { main: '#e8734f' },
    success: { main: '#6fcf97' },
  },
  shape: { borderRadius: 12 },
});
